# Setup:
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
#   # PyPI's default `torch` wheel on Windows is CPU-only -- reinstall it from
#   # the CUDA index to get GPU acceleration for marker_fallback_parse.py:
#   .venv\Scripts\pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118
#
# Without a CUDA GPU, skip that last step -- marker will run on CPU (much
# slower, but functional) and pdf_parser.py doesn't need a GPU at all.

arxiv
requests
pymupdf
beautifulsoup4
tiktoken
# Pinned to 1.x: marker-pdf 2.0.0 requires spawning a vLLM or llama.cpp model
# server (Docker on GPU machines) even for local inference. 1.x runs surya's
# OCR/layout models directly in-process (transformers/torch), no server needed.
marker-pdf==1.10.2

# Retrieval stack (embeddings, vector index, lexical search, reranking).
faiss-cpu
rank_bm25
# Pinned: sentence-transformers>=5.0 requires transformers>=5.0, which
# conflicts with marker-pdf's transformers<5.0 pin. 4.1.0 is the last release
# before that jump and has everything needed (SentenceTransformer, CrossEncoder).
sentence-transformers==4.1.0

# Structured extraction (structured_extract.py) and Q&A (qa.py). Free Gemini
# API key from https://aistudio.google.com/apikey. Put it in a .env file at
# the project root as GEMINI_API_KEY=... (gitignored) -- llm_utils.py loads
# it automatically, no need to export it in your shell.
google-genai
python-dotenv

# Q&A UI (app.py)
streamlit
